import oracledb from 'oracledb'; import {getConnection} from '../../../../config/database'; import type{Anticipo,CreateAnticipoInput,UpdateAnticipoInput}from'@erp/contracts';
interface Row{ID_ANTICIPO:number;ID_CLIENTE:number;ID_PAGO:number|null;MONTO_ORIGINAL:number;MONTO_DISPONIBLE:number;FECHA:Date;ESTADO:string} const map=(r:Row):Anticipo=>({idAnticipo:r.ID_ANTICIPO,idCliente:r.ID_CLIENTE,idPago:r.ID_PAGO,montoOriginal:r.MONTO_ORIGINAL,montoDisponible:r.MONTO_DISPONIBLE,fecha:r.FECHA?.toISOString()??'',estado:r.ESTADO});
export async function findAll({page,limit,search}:{page:number;limit:number;search?:string}){const c=await getConnection();try{const w=search?`WHERE UPPER(ESTADO) LIKE UPPER(:search) OR TO_CHAR(ID_ANTICIPO) LIKE :search OR TO_CHAR(ID_CLIENTE) LIKE :search`:'';const sb=search?{search:`%${search}%`}:{ };const d=await c.execute<Row>(`SELECT ID_ANTICIPO,ID_CLIENTE,ID_PAGO,MONTO_ORIGINAL,MONTO_DISPONIBLE,FECHA,ESTADO FROM CXC_ANTICIPOS ${w} ORDER BY FECHA DESC,ID_ANTICIPO DESC OFFSET :offset ROWS FETCH NEXT :limit ROWS ONLY`,{...sb,offset:(page-1)*limit,limit});const n=await c.execute<{TOTAL:number}>(`SELECT COUNT(*) TOTAL FROM CXC_ANTICIPOS ${w}`,sb);return{data:(d.rows??[]).map(map),total:n.rows?.[0]?.TOTAL??0};}finally{await c.close();}}
export async function findById(id:number){const c=await getConnection();try{const r=await c.execute<Row>(`SELECT ID_ANTICIPO,ID_CLIENTE,ID_PAGO,MONTO_ORIGINAL,MONTO_DISPONIBLE,FECHA,ESTADO FROM CXC_ANTICIPOS WHERE ID_ANTICIPO=:id`,{id});return r.rows?.[0]?map(r.rows[0]):null;}finally{await c.close();}}
export async function create(i:CreateAnticipoInput){const c=await getConnection();try{const r=await c.execute<{id:number[]}>(`INSERT INTO CXC_ANTICIPOS(ID_CLIENTE,ID_PAGO,MONTO_ORIGINAL,MONTO_DISPONIBLE,FECHA,ESTADO) VALUES(:idCliente,:idPago,:montoOriginal,:montoDisponible,TO_DATE(:fecha,'YYYY-MM-DD'),:estado) RETURNING ID_ANTICIPO INTO :id`,{...i,idPago:i.idPago??null,id:{dir:oracledb.BIND_OUT,type:oracledb.NUMBER}});await c.commit();return r.outBinds!.id[0];}catch(e){await c.rollback();throw e;}finally{await c.close();}}
export async function update(id:number,i:UpdateAnticipoInput){const m:any={id};const f:string[]=[];for(const[k,col]of[['idCliente','ID_CLIENTE'],['idPago','ID_PAGO'],['montoOriginal','MONTO_ORIGINAL'],['montoDisponible','MONTO_DISPONIBLE'],['estado','ESTADO']] as const){if(i[k]!==undefined){f.push(`${col}=:${k}`);m[k]=i[k];}}if(i.fecha!==undefined){f.push(`FECHA=TO_DATE(:fecha,'YYYY-MM-DD')`);m.fecha=i.fecha;}if(!f.length)return;const c=await getConnection();try{await c.execute(`UPDATE CXC_ANTICIPOS SET ${f.join(',')} WHERE ID_ANTICIPO=:id`,m);await c.commit();}catch(e){await c.rollback();throw e;}finally{await c.close();}}
export async function remove(id:number){const c=await getConnection();try{await c.execute(`DELETE FROM CXC_ANTICIPOS WHERE ID_ANTICIPO=:id`,{id});await c.commit();}catch(e){await c.rollback();throw e;}finally{await c.close();}}
