// Forma mínima para poblar un <Select> con datos reales de la base
// (cliente, empleado, documento, etc.) sin exponer toda la fila.
export interface CatalogoOption {
  id: number;
  label: string;
}
